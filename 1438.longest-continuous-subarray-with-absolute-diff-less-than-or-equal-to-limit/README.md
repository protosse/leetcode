这道题可以用[滑动窗口](../76.minimum-window-substring/)来解决，中途算绝对值然后移动left。

难点其实在于如何快速有效的判断绝对值是否符合要求，暴力的解法就是 left 到 right 循环遍历，但是这样的时间复杂度是到 $o(n^2)$ 的，肯定会超时

一个比较有效的方法是使用两个队列来实现，一个单调递增的队列维护最小值，一个单调递减的队列维护最大值。这样我们只需要计算两个队列的队首的差值，即可知道当前窗口是否满足条件