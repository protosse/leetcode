如果合并后的数组个数是奇数的话，那么中位数就是中间的数字，是偶数的话，那么中位数就是中间两个数字的平均值

最笨的方法当然是合并两个数组，然后排序。但是没有必要，用一个循环去找就行了，循环次数为 总数/2，相当于只归并一半，用两个 index 去取两个数组中的值，比较大小，小的 index 就后移一位，并记录当前的值，最后根据奇数和偶数分别算出中位数

但是这个方法时间复杂度还是 O(m+n)

看到过那个分割线的解法，属实有点难懂，相比之下求第k小的解法更直观一点。